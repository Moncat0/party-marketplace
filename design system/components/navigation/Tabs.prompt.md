Section switcher — vendor profile tabs (Overview / Packages / Reviews), dashboard sections.

```jsx
<Tabs tabs={['Overview','Packages','Reviews']} active={tab} onChange={setTab}/>
```

Active tab gets a 3px orange underline; inactive tabs are muted ink-500.
