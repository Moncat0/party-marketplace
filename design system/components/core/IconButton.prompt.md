Round icon-only button, minimal — ghost by default (no fill until hover).

```jsx
<IconButton icon={<HeartIcon/>} variant="ghost" label="Save vendor"/>
```

`ghost` (default) for toolbars, `outline` (thin border) on light surfaces, `solid` (black fill) on photos. Always pass `label` for accessibility.
