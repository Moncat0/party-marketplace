Small soft-tint pill for status or category (e.g. "Chef's pick", "Available") — no heavy border, tinted background instead.

```jsx
<Badge tone="primary">Chef's pick</Badge>
```

Tones: `neutral` (cream), `primary` (soft orange), `secondary` (soft pink), `success`, `outline` (thin border, for filters).
