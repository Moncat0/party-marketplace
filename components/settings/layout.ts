/** Account-settings layout — panel widths; header chrome lives in siteChrome.ts */

export const settingsLayout = {
  /** Left nav column (px) */
  navWidth: 320,
  /** Personal-info form + tip column (px) */
  formMax: 680,
  /** Gap between divider and form (px) */
  panelGap: 48,
} as const
